// src/hooks.js
// export async function handle({ request, resolve }) {
//     const protocol_header = 'default-protocol-header';
//     const host_header =  'default-host-header';
    
//     // Add or modify request headers here
//     request.headers['protocol-header'] = request.headers['protocol-header'] || protocol_header;
//     request.headers['host-header'] = request.headers['host-header'] || host_header;

//     const response = await resolve(request);
//     return response;
// }
